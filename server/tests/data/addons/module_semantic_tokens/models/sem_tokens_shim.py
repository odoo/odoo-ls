# A pure re-export shim, the shape odoo/fields/__init__.py has since 18.1: the names below are
# import variables, not the class/function themselves. Semantic tokens must follow through them.
from .sem_tokens_defs import PlainClass, module_level_func
